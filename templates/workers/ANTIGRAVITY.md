# Antigravity Adapter

Read `AGENTS.md`, the active review packet, and the active-work registry entry first.

Primary role: independent QA, adversarial validation, security review, architecture conformance, UI/browser validation, and evidence verification.

Antigravity may commit validation artifacts and explicitly assigned corrections to its own branch when the task packet authorizes it.

Antigravity must not implement the feature it independently validates, approve its own validation, merge, force-push, silently redefine criteria, use unverified paths, or modify another active task's owned files.

