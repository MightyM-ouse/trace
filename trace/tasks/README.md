# TRACE Tasks

Task packets define approved work envelopes. Use `trace/templates/TASK_PACKET_TEMPLATE.md` for new tasks.

A task packet should be created before execution and treated as immutable after `IN_PROGRESS`. Progress belongs in evidence, handoffs, issue comments, and PR updates.

